import * as log from './logger';

// Both non-prod and prod apps are registered in Lilly production Azure AD.
const issuer = process.env.OPENID_ISSUER || 'https://login.microsoftonline.com/18a59a81-eea8-4c30-948a-d8824cdc2580/v2.0';
log.info(`Using OPENID_ISSUER ${issuer}`);

export default issuer;
